export class Filter {

    constructor(category, items) {
        this.category = category;
        this.items = items;
        //this.items = items.map ( (item) => new Tag);

        this.$wrapper = document.getElementById("filters");
    }

    render(){

    }

}